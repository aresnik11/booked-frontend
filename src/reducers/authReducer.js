import {  } from '../types'

const defaultState = {
    user: {}
}

function authReducer(state = defaultState, action) {
    switch(action.type) {
        default:
            return state
    }
}

export default authReducer